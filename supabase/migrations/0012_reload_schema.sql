-- ============================================================================
--  0012 — PostgREST 스키마 캐시 강제 갱신
--  "Could not find the function ... in the schema cache" 오류가 날 때 실행.
--  (함수/컬럼을 바꾼 뒤 PostgREST 가 아직 옛 스키마를 캐시하고 있을 때)
-- ============================================================================

notify pgrst, 'reload schema';
